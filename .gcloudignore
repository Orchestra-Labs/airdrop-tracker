# This file specifies files that are *not* uploaded to Google Cloud
# using gcloud. It follows the same syntax as .gitignore.

# Ignore git directory
.git
.gitignore

# Node modules (App Engine will run yarn install)
node_modules/

# Build output (App Engine will run yarn build)
dist/

# Development files
.vscode/
.idea/
*.log

# Other env files (but NOT .env - we need that for the build)
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.prod

# OS files
.DS_Store
Thumbs.db
